/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.ytimg.com'], // {{ edit_1: Added i.ytimg.com to the allowed image domains }}
    },
};

export default nextConfig;
